= simple_form_for([category,product], :html => {:class => 'form-horizontal' }, wrapper: :horizontal_form) do |f|
  = f.error_notification
  = f.input :title
  = f.input :description
  = f.input :price
  .pull-right= f.button :submit